import pandas as pd
import os
sampleinfo = pd.read_table(config["sampleTable"],dtype={"sampleid": str})


SAMPLES=list(sampleinfo.loc[:,"sampleid"])


rule all:
    input:
        expand("results/seurat_filtered/filtered_seurat_{sample}.rds",sample=SAMPLES)
    default_target: True

include: "rules/tenx2seuratrds.smk"
